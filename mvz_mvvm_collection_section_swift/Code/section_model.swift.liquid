{% include 'header' %}

import Foundation
import VMCollection

class {{ prefix }}{{ module_info.name }}SectionModel: VMCollectionViewSectionModel {
    
    var sectionCellModels = [{{ prefix }}{{ module_info.name }}CellModel]() {
        didSet {
            cellModels = sectionCellModels
        }
    }
}