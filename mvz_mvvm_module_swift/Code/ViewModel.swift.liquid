{% include 'header' %}

protocol {{ prefix }}{{ module_info.name }}ViewModelOutput: class {
    func update()
}

final class {{ prefix }}{{ module_info.name }}ViewModel {

    private let interactor: {{ prefix }}{{ module_info.name }}Interactor
    private unowned var output: {{ prefix }}{{ module_info.name }}ViewModelOutput
    
	// MARK: - Lifecycle

    init(interactor: {{ prefix }}{{ module_info.name }}Interactor, output: {{ prefix }}{{ module_info.name }}ViewModelOutput) {
        self.interactor = interactor
       	self.output = output
    }

    func viewDidLoad() {
        output.update()
    }
}
