{% include 'header' %}

#import <UIKit/UIKit.h>

@protocol {{ prefix }}{{ module_info.name }}ModelProtocol;

@interface {{ prefix }}{{ module_info.name }} : UIView

@property (nonatomic) id<{{ prefix }}{{ module_info.name }}ModelProtocol> viewModel;

@end
