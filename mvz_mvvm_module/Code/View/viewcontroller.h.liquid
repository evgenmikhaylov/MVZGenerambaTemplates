{% include 'header' %}

#import <UIKit/UIKit.h>
@protocol {{ prefix }}{{ module_info.name }}ViewModelProtocol;

@interface {{ prefix }}{{ module_info.name }}ViewController : UIViewController

@property (nonatomic) id<{{ prefix }}{{ module_info.name }}ViewModelProtocol> viewModel;

@end
