{% include 'header' %}

import Foundation

final class {{ prefix }}{{ module_info.name }}Interactor {

    typealias Dependencies = Any

    private let dependencies: Dependencies

    init(dependencies: Dependencies = NSObject()) {
        self.dependencies = dependencies
    }

}
